name: DTAP Pipeline

on:
  push:
    branches:
      - develop
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    outputs:
      image_tag: ${{ steps.meta.outputs.image_tag }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Build and Test with Maven
        run: mvn clean verify

      - name: Generate image tag
        id: meta
        run: echo "image_tag=${GITHUB_SHA}" >> $GITHUB_OUTPUT

  deploy-dev:
    if: github.ref == 'refs/heads/develop'
    needs: build
    runs-on: ubuntu-latest
    environment: development

    steps:
      - name: Deploy to Development
        run: echo "Deploying ${{ needs.build.outputs.image_tag }} to DEV"

  deploy-test:
    if: github.ref == 'refs/heads/develop'
    needs: deploy-dev
    runs-on: ubuntu-latest
    environment: test

    steps:
      - name: Deploy to Test
        run: echo "Deploying ${{ needs.build.outputs.image_tag }} to TEST"

  deploy-acceptance:
    if: github.ref == 'refs/heads/develop'
    needs: deploy-test
    runs-on: ubuntu-latest
    environment: acceptance

    steps:
      - name: Deploy to Acceptance
        run: "Deploying ${{ needs.build.outputs.image_tag }} to ACCEPTANCE"

  deploy-production:
    if: github.ref == 'refs/heads/main'
    needs: build
    runs-on: ubuntu-latest
    environment:
    name: production

    steps:
      - name: Deploy to Production
        run: echo "Deploying ${{ needs.build.outputs.image_tag }} to PRODUCTION"
